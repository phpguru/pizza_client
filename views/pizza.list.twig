{% extends 'layout.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-1 col-lg-offset-11">
                <a class="btn btn-orange" href="/pizza/create" role="button">Create a Pizza</a>
            </div>
            <div class="col-lg-10 col-lg-offset-1">
                <h1>Pizza List</h1>
                <ul>
                    {% for pizza in pizzas %}
                        {% if pizza.name is not empty %}
                            {% if pizza.id in mypizzas %}
                                <li>{{ pizza.id }}) <strong><a href="/pizza/edit/{{pizza.id}}">{{ pizza.name|lower|title }}</a></strong> <em>{{ pizza.description }}</em>
                            {% else %}
                                <li>{{ pizza.id }}) <strong>{{ pizza.name|lower|title }}</strong> <em>{{ pizza.description }}</em>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="col-lg-1 col-lg-offset-11">
                <a class="btn btn-orange" href="/pizza/create" role="button">Create a Pizza</a>
            </div>
        </div>
    </div>
{% endblock %}
